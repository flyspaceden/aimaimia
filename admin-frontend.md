# 管理后台前端整改方案

## 1. 文档目标

本文档用于指导管理后台前端整改，解决以下问题：

- 左侧目录层级混乱，页面归属不清
- 系统配置、VIP 配置、普通奖励配置边界不清
- 部分页面的信息密度失衡，主次不分
- 部分页面存在右侧内容溢出、表格过宽、详情堆叠的问题
- 存在已开发但未挂菜单/未挂路由的页面，信息架构不完整

本文档基于当前 `admin/` 代码逐页审阅整理，不是抽象建议，而是面向现有代码结构的整改计划。

---

## 2. 现状诊断

### 2.1 菜单层面

当前菜单定义在 `admin/src/layouts/AdminLayout.tsx`，主要问题：

- 一级菜单按“技术模块”堆叠，不按“运营任务流”组织
- `系统配置` 混入 VIP 参数、抽奖参数、定价参数、订单参数，边界失控
- `会员/奖励` 下同时放会员、提现、树、普通系统配置，但缺少 VIP 配置页
- `退款仲裁` 页面已存在，但没有挂路由/菜单
- `broadcast-window` 已废弃但仍保留路由，容易形成隐性入口

### 2.2 页面层面

当前页面大体可分为六类：

- 总览：工作台
- 用户奖励：用户、会员、提现、奖励树、奖励配置
- 商家商品：企业、商家商品、奖励商品、溯源
- 交易售后：订单、换货、退款、运费
- 运营活动：红包、抽奖
- 系统管理：配置、管理员、角色、审计

现在的问题不是页面数量多，而是页面职责边界不统一。

### 2.3 布局层面

当前多个页面使用：

- `div style={{ padding: 24 }}`
- 大宽度表格但未统一配置 `scroll.x`
- 复杂页直接使用多列布局，缺少页面最大宽度和响应式收缩策略

因此会出现：

- 红包活动、抽奖管理、企业详情、树结构页在窄屏或侧边栏展开后易拥挤
- 表格列过多时挤压右侧内容
- 详情页信息块过多，阅读顺序不稳定

### 2.4 权限与角色视图层面

当前管理后台缺少多角色视图的考量：

- 所有管理员看到的菜单和页面内容完全一致，没有按角色差异化
- 后端已有 `PermissionGuard` + `@Permission()` 装饰器，但前端未充分利用 `PermissionGate` 组件做按钮级控制
- 超级管理员、运营、客服、财务等角色的菜单可见范围未定义
- 高频列表页缺少批量操作能力，影响运营效率（批量审核、批量导出、批量发货等属于 P2/P3 扩展项，需后端配套接口、权限模型和导出任务系统，不在前端整改范围内，单独排期）

---

## 3. 整改原则

### 3.1 信息架构原则

- 一级菜单按业务任务组织，不按技术模块组织
- 列表页进入菜单，详情页默认不进入菜单
- “配置类页面”必须按业务域拆分，不能继续聚合到一个万能配置页
- 同一业务域内，列表页、详情页、配置页命名风格统一

### 3.2 页面设计原则

- 每个页面只解决一个核心任务
- 页面头部固定为：标题 + 简介 + 主操作
- 列表页优先表格，详情页优先卡片分组
- 表格默认考虑横向滚动和列优先级
- 大页面优先拆 Tab 或拆子页面，不在单页中塞入过多业务

### 3.3 交互反馈原则

- 涉及资金、状态变更的操作（提现审核、封禁用户、退款仲裁、配置变更）必须二次确认弹窗
- 普通 CRUD 操作使用 `message.success` 轻提示，不弹窗
- 列表页操作成功后自动刷新当前页，不跳转
- 乐观更新仅限低风险操作（如标记已读），资金/审核类操作必须等待服务端确认
- 高频列表页（用户、订单、商品、企业、提现）后续应支持批量操作（P2/P3 扩展项，需后端配套批量接口和导出任务系统）

### 3.4 实施原则

- 先调整菜单和页面归位，再做 UI 优化
- 先做配置边界拆分，再做内容精修
- 先修高频页面，再修低频页面

---

## 4. 目标左侧目录结构

建议将管理后台左侧目录调整为：

```text
工作台

用户与奖励
  用户管理
  VIP会员
  提现审核
  VIP奖励树
  普通奖励树
  VIP系统配置
  普通系统配置

商家与商品
  企业管理
  商家商品
  奖励商品
  溯源批次

交易与售后
  订单管理
  换货仲裁
  退款仲裁
  运费规则

运营活动
  红包管理          ← 单入口，内部 Tab 切换（活动/发放/使用/统计）
  抽奖管理

系统管理
  平台设置
  管理员账号
  角色权限
  审计日志
```

### 4.1 不进入菜单的页面

- 登录页
- 商品编辑页
- 订单详情页
- 企业详情页
- 会员详情页
- 奖励商品编辑页

### 4.2 需要补齐的页面/入口

- 新增 `VIP系统配置` 页面
- 将现有 `系统配置` 改名为 `平台设置`
- 将 `退款仲裁` 正式挂入路由与菜单
- 将 `broadcast-window` 归档，不再对运营暴露

---

## 5. 页面归位方案

| 页面 | 路由 | 建议菜单位置 | 说明 |
| --- | --- | --- | --- |
| 登录页 | `/login` | 不进菜单 | 独立登录入口 |
| 工作台 | `/` | 工作台 | 总览与待办中心 |
| 用户管理 | `/users` | 用户与奖励 / 用户管理 | 买家账户管理 |
| VIP会员 | `/bonus/members` | 用户与奖励 / VIP会员 | 会员列表入口 |
| 会员详情 | `/bonus/members/:userId` | 不进菜单 | 从列表进入 |
| 提现审核 | `/bonus/withdrawals` | 用户与奖励 / 提现审核 | 奖励提现审核 |
| VIP奖励树 | `/bonus/vip-tree` | 用户与奖励 / VIP奖励树 | VIP树查看 |
| 普通奖励树 | `/bonus/normal-tree` | 用户与奖励 / 普通奖励树 | 普通树查看 |
| VIP系统配置 | 新增 `/bonus/vip-config` | 用户与奖励 / VIP系统配置 | 从旧系统配置拆出 |
| 普通系统配置 | `/bonus/normal-config` | 用户与奖励 / 普通系统配置 | 保持独立 |
| 企业管理 | `/companies` | 商家与商品 / 企业管理 | 商家审核与维护 |
| 企业详情 | `/companies/:id` | 不进菜单 | 从企业列表进入 |
| 商家商品 | `/products` | 商家与商品 / 商家商品 | 商家提交商品审核管理 |
| 商品编辑 | `/products/:id/edit` | 不进菜单 | 从商品列表进入 |
| 奖励商品 | `/reward-products` | 商家与商品 / 奖励商品 | 平台运营商品 |
| 奖励商品编辑 | `/reward-products/:id/edit` | 不进菜单 | 从奖励商品列表进入 |
| 溯源批次 | `/trace` | 商家与商品 / 溯源批次 | 溯源批次维护 |
| 订单管理 | `/orders` | 交易与售后 / 订单管理 | 订单主入口 |
| 订单详情 | `/orders/:id` | 不进菜单 | 从订单列表进入 |
| 换货仲裁 | `/replacements` | 交易与售后 / 换货仲裁 | 售后仲裁 |
| 退款仲裁 | `/refunds` | 交易与售后 / 退款仲裁 | 现有页面补路由 |
| 运费规则 | `/shipping-rules` | 交易与售后 / 运费规则 | 配置页 |
| 红包管理 | `/coupons` | 运营活动 / 红包管理 | 单入口，内部 Tab 切换（活动/发放/使用/统计） |
| 抽奖管理 | `/lottery` | 运营活动 / 抽奖管理 | 后续可再拆 |
| 平台设置 | `/config` | 系统管理 / 平台设置 | 仅保留平台级参数 |
| 管理员账号 | `/admin/users` | 系统管理 / 管理员账号 | 后台账号 |
| 角色权限 | `/admin/roles` | 系统管理 / 角色权限 | 权限矩阵 |
| 审计日志 | `/audit` | 系统管理 / 审计日志 | 操作追踪 |

---

## 6. 逐页优化方案

以下优化方案按“页面职责、当前问题、整改方向”组织。

### 6.1 登录页 ✅ 已完成

**职责**

- 管理员登录

**当前问题**

- 展示默认账号密码，不适合正式环境
- 缺少环境标识

**整改方向**

- 去掉默认账号文案
- 增加“测试环境/正式环境”标识
- 登录失败提示改为更明确的错误原因

---

### 6.2 工作台 ✅ 已完成（侧边栏徽标待做）

**职责**

- 平台总览
- 运营待办入口

**当前问题**

- 以统计卡片为主，缺少行动导向
- 高优先任务没有集中展示

**整改方向**

- 保留基础指标
- 增加待处理事项卡片：
  - 待审核商品
  - 待审核企业
  - 待处理提现
  - 待处理换货/退款
- 增加快捷入口区
- 图表弱化，待办强化
- **数据来源**：后端需新增 `GET /admin/dashboard/summary` 聚合接口，一次返回各模块待处理数量
- **刷新策略**：页面可见时每 60 秒轮询刷新待办数字，左侧菜单对应项同步显示数字徽标
- 待办数字徽标在左侧菜单的"用户与奖励""商家与商品""交易与售后"上展示未处理总数

---

### 6.3 用户管理 ✅ 已完成

**职责**

- 管理买家账户、状态和风险用户

**当前问题**

- 只有基础列表，缺少用户上下文

**整改方向**

- 增加最近下单时间、累计消费、是否 VIP
- 增加筛选：
  - 正常/封禁
  - VIP/普通
  - 高活跃/低活跃
- 封禁时补封禁原因
- 后续增加用户详情抽屉

---

### 6.4 VIP会员 ✅ 已完成

**职责**

- 管理 VIP 用户与奖励成员

**当前问题**

- 字段偏少，只看到静态身份信息

**整改方向**

- 列表补充：
  - 可用余额
  - 冻结金额
  - 奖励树层级
  - 自购次数
- 默认筛选应偏向 VIP
- 将“VIP会员”名称与“用户管理”明确区分

---

### 6.5 会员详情 ✅ 已完成

**职责**

- 查看会员基础信息、树位置、奖励流水、提现记录

**当前问题**

- 内容已经比较完整，但信息层级还不够清晰

**整改方向**

- 拆为四段：
  - 基础信息
  - 树结构信息
  - 钱包统计
  - 流水与提现
- 增加“跳转到 VIP 树 / 普通树定位该用户”
- 增加“最近订单”与“最近奖励来源”

---

### 6.6 提现审核 ✅ 已完成

**职责**

- 审核奖励提现申请

**当前问题**

- 信息量不足，审核依据不够

**整改方向**

- 增加提现渠道、账户尾号、申请备注
- 增加用户累计提现金额、最近提现次数
- 审核弹窗中展示风险信息
- 【P2/P3 扩展·需后端配套】支持批量审核通过/拒绝（全选/跨页选择）
- 【P2/P3 扩展·需后端配套】批量导出提现记录（CSV/Excel）

---

### 6.7 VIP奖励树 ✅ 已完成

**职责**

- 可视化查看 VIP 奖励树结构

**当前问题**

- 页面复杂度高
- 左右布局固定，窄屏下容易压缩

**整改方向**

- 保持搜索 + 树 + 详情的三段结构
- 详情面板做可折叠
- 缩放、深度、回到当前节点操作固定在顶部工具条
- 当前节点、父节点、子节点视觉主次再拉开

---

### 6.8 普通奖励树 ✅ 已完成

**职责**

- 可视化查看普通奖励树结构

**当前问题**

- 与 VIP 树结构一致，但当前命名和视觉识别还不够强

**整改方向**

- 与 VIP 树保持同组件结构
- 绿色主题保留
- 顶部明确标注“普通奖励体系”

---

### 6.9 VIP系统配置 ✅ 已完成

**职责**

- 管理 VIP 奖励体系参数

**当前问题**

- 当前不存在独立页
- 相关参数混在 `系统配置` 中

**整改方向**

- 从现有 `config/index.tsx` 拆出：
  - `VIP_PLATFORM_PERCENT`（平台利润，默认 50%）
  - `VIP_REWARD_PERCENT`（奖励池，默认 30%）
  - `VIP_INDUSTRY_PERCENT`（产业基金，默认 10%）
  - `VIP_CHARITY_PERCENT`（慈善基金，默认 2%）
  - `VIP_TECH_PERCENT`（科技基金，默认 2%）
  - `VIP_RESERVE_PERCENT`（备用金，默认 6%）
  - `VIP_PRICE`
  - `VIP_MIN_AMOUNT`
  - `VIP_MAX_LAYERS`
  - `VIP_BRANCH_FACTOR`
  - `VIP_REFERRAL_BONUS`
  - `VIP_FREEZE_DAYS`
  - `VIP_REWARD_EXPIRY_DAYS`
- 页面结构参考 `普通系统配置`
- 分组建议：
  - 分润比例
  - VIP 基础设置
  - VIP 奖励有效期设置

---

### 6.10 普通系统配置 ✅ 已完成

**职责**

- 管理普通奖励体系参数

**当前问题**

- 页面结构已经合理
- 缺少风险说明与推荐值

**整改方向**

- 保留现有结构
- 每组增加“业务说明”
- 对六分比例增加“推荐模板”和“恢复默认值”
- 增加变更影响提示

---

### 6.11 企业管理 ✅ 已完成

**职责**

- 企业审核与企业信息维护

**当前问题**

- 列表偏基础
- 审核入口和待办感不够强

**整改方向**

- 增加待审核 tab
- 增加资质状态、创始人状态、商品数
- 列表页突出 `PENDING`
- 将审核操作和查看详情分开
- 【P2/P3 扩展·需后端配套】支持批量审核通过/拒绝

---

### 6.12 企业详情 ✅ 已完成

**职责**

- 查看并维护企业全量信息

**当前问题**

- 信息很多，但分区可读性仍可提升

**整改方向**

- 固定为五块：
  - 基础信息
  - 企业亮点
  - 员工信息
  - 资质文档
  - 审核操作
- 文档区增加审核状态筛选
- 创始人绑定区单独突出

---

### 6.13 商家商品 ✅ 已完成

**职责**

- 审核与管理商家提交商品

**当前问题**

- 列表和审核弹窗已有雏形
- 缺少更强的审核工作流

**整改方向**

- 保留 `全部商品 / 待审核` tab
- 列表补充：
  - 分类
  - 库存
  - 企业状态
- 审核弹窗增加商品主图和简要属性
- 【P2/P3 扩展·需后端配套】支持批量审核通过/拒绝

---

### 6.14 商品编辑 ✅ 已完成

**职责**

- 编辑商家商品信息

**当前问题**

- 页面完整，但偏“表单堆叠”

**整改方向**

- 调整顺序：
  - 状态
  - 基础信息
  - 图片
  - SKU
  - 审核历史
- 保存按钮固定在页头
- 补“审核记录”区块

---

### 6.15 奖励商品 ✅ 已完成

**职责**

- 管理平台自营或平台运营商品

**当前问题**

- 列表基本可用，但运营感不强

**整改方向**

- 增加库存预警筛选
- 增加毛利展示
- 增加状态快捷筛选

---

### 6.16 奖励商品编辑 ✅ 已完成

**职责**

- 编辑奖励商品和 SKU

**当前问题**

- 信息较多，操作分散

**整改方向**

- 基础信息与 SKU 分区保持
- 增加总库存、最低价、最高价汇总卡
- SKU 弹窗中增加利润提示

---

### 6.17 溯源批次 ✅ 已完成

**职责**

- 管理溯源批次

**当前问题**

- 直接输入 `companyId`，不适合运营使用

**整改方向**

- 改为企业选择器
- 增加批次状态和关联商品数
- 列表增加企业筛选

---

### 6.18 订单管理 ✅ 已完成

**职责**

- 管理订单和发货流程

**当前问题**

- 列表信息偏少
- 售后上下文缺失

**整改方向**

- 增加订单来源、支付状态、售后状态
- 增加状态快捷筛选
- 发货弹窗增加常用快递模板
- 【P2/P3 扩展·需后端配套】支持批量导出订单（CSV/Excel）
- 【P2/P3 扩展·需后端配套】支持批量发货（模板导入快递单号）

---

### 6.19 订单详情 ✅ 已完成

**职责**

- 查看订单完整信息

**当前问题**

- 当前只覆盖基础信息、商品、物流、地址

**整改方向**

- 增加：
  - 支付信息
  - 优惠信息
  - 售后记录
  - 状态流转记录
- 面包屑保留

---

### 6.20 换货仲裁 ✅ 已完成

**职责**

- 仲裁换货申请

**当前问题**

- 已有凭证预览和仲裁弹窗，结构基础不错

**整改方向**

- 增加卖家处理记录
- 增加申请人地址与换货目标信息
- 仲裁结果增加模板文案

---

### 6.21 退款仲裁 ✅ 已完成

**职责**

- 仲裁退款申请

**当前问题**

- 页面已存在，但没有进入导航体系

**整改方向**

- 正式接入路由和菜单
- 与换货仲裁统一到“交易与售后”
- 增加退款路径、支付渠道、卖家处理记录

---

### 6.22 运费规则 ✅ 已完成

**职责**

- 管理运费规则和运费预览

**当前问题**

- 结构正确，但可操作性一般

**整改方向**

- 编辑弹窗增加规则冲突检测提示
- 预览区加入地区选择器
- 列表突出优先级命中逻辑

---

### 6.23 红包管理（合并为单入口 + Tab） ✅ 已完成

**职责**

- 管理红包活动生命周期、发放明细、核销记录、效果统计

**当前问题**

- 原方案将红包拆成 4 个菜单项（活动/发放记录/使用记录/统计），菜单项偏多，导致左侧菜单膨胀
- 红包活动列表列较多，主次信息混杂，容易发生横向拥挤和右侧溢出
- 发放记录缺少活动维度筛选
- 使用记录筛选维度不足
- 统计图表标签在窄宽下易拥挤

**整改方向**

合并为 `/coupons` 单入口，内部使用 Tab 切换（与抽奖管理保持一致的交互模式）：

**Tab 1：红包活动**
- 表格加 `scroll={{ x: ... }}` 统一横向滚动
- 将字段改成”两层信息”：
  - 主信息：活动名、状态、活动时间
  - 副信息：触发方式、发放方式、抵扣规则、进度
- 操作按钮收敛到”更多”
- 详情不要只用弹窗，后续可考虑独立详情抽屉

**Tab 2：发放记录**
- 增加按活动、状态、即将过期筛选
- 使用订单支持直接跳转详情

**Tab 3：使用记录**
- 增加活动筛选
- 增加订单状态联动
- 支持按用户或订单搜索

**Tab 4：统计**
- 控制横轴标签数量
- 长活动名在图表中缩写显示
- KPI 区增加”核销率趋势解释”

---

### 6.27 抽奖管理 ✅ 已完成

**职责**

- 管理奖池、抽奖记录、统计

**当前问题**

- 单页承载三个子任务，复杂度过高
- 智能概率编辑器本身结构较重

**整改方向**

- 短期保留单入口 + tab
- 中期建议拆为：
  - 奖池管理
  - 抽奖记录
  - 抽奖统计
- `智能概率编辑` 弹窗加宽，并为窄屏提供纵向降级布局

---

### 6.28 平台设置 ✅ 已完成

**职责**

- 管理平台级公共参数

**当前问题**

- 当前 `系统配置` 是大杂烩

**整改方向**

- 将页面改名为 `平台设置`
- 仅保留：
  - `MARKUP_RATE`
  - `DEFAULT_SHIPPING_FEE`
  - `LOTTERY_ENABLED`
  - `LOTTERY_DAILY_CHANCES`
  - `AUTO_CONFIRM_DAYS`
- 所有 VIP 参数迁出
- 所有普通奖励参数继续留在普通配置页

---

### 6.29 管理员账号 ✅ 已完成

**职责**

- 管理后台管理员账号

**当前问题**

- 基础 CRUD 足够，但缺少安全上下文

**整改方向**

- 增加最近登录 IP
- 增加是否超级管理员标识
- 增加禁用原因

---

### 6.30 角色权限 ✅ 已完成

**职责**

- 管理角色和权限矩阵

**当前问题**

- 权限矩阵可用，但操作效率一般

**整改方向**

- 增加模块全选
- 增加“复制角色”
- 增加角色成员数展示

---

### 6.31 审计日志 ✅ 已完成

**职责**

- 查看后台操作记录，支持回滚

**当前问题**

- 已有详情和回滚，但缺少跨页面联动

**整改方向**

- 增加目标对象跳转
- 增加影响说明
- 回滚前提示受影响数据范围
- **反向联动**：在其他页面的高风险操作区域（审核、封禁、配置变更、提现处理）统一增加"查看操作记录"入口，快速定位"谁在什么时候做了这个操作"

---

### 6.32 普通广播窗口 ✅ 已归档（不进菜单）

**职责**

- 历史遗留普通广播机制可视化

**当前问题**

- 页面仍存在，但菜单已隐藏
- 与当前主信息架构不一致

**整改方向**

- 标记为归档页
- 不再进菜单
- 仅技术排查时保留内部访问

---

## 7. 高优先级整改项

### P0：必须先做 — ✅ 全部完成

1. ✅ 重构左侧目录结构（6 大分组 + layout="side"）
2. ✅ 新增 `VIP系统配置` 页面（12 项配置、3 分组、合计校验、版本历史）
3. ✅ 将 `系统配置` 收敛为 `平台设置`（仅保留 5 项平台级参数）
4. ✅ 将 `退款仲裁` 挂入路由和菜单
5. ✅ 红包管理页（单入口 Tab）补横向滚动和列重排，解决右侧溢出

### P1：第二阶段 — ⚠️ 大部分完成

1. ✅ 工作台改造成待办中心（5 项待办卡片 + 60s 轮询；❗ 侧边栏菜单徽标未实现）
2. ✅ 企业管理（待审核 tab + Badge）、✅ 商品管理（待审核 tab + 分类/库存/企业状态列）
3. ✅ 订单详情（支付信息 + 优惠 + Steps 状态流转）、✅ 会员详情（四段拆分 + 树跳转）
4. ❌ 抽奖管理未优化

### P2：第三阶段 — ✅ 全部完成

1. ✅ 用户详情抽屉（Drawer + VIP/普通筛选 + scroll.x）
2. ✅ 角色复制、权限全选（复制按钮 + 模块全选 + 成员数）
3. ✅ 审计日志跨页面联动（getTargetUrl 跳转 + scroll.x）
4. ✅ 溯源页企业选择器化（Select + 搜索 + 加载态）

---

## 8. 推荐实施顺序

### 阶段一：信息架构重组 — ✅ 完成

- ✅ 修改菜单结构
- ✅ 修改路由归位
- ✅ 新增 VIP 配置页
- ✅ 调整系统配置页为平台设置
- ✅ 接入退款仲裁

### 阶段二：高频页面优化 — ✅ 完成

- ✅ 工作台（待办卡片已实现；侧边栏徽标待做）
- ✅ 红包管理页（单入口 Tab + scroll.x）
- ✅ 企业管理（待审核 tab + Badge + 商品数）
- ✅ 商品管理（待审核 tab + 分类/库存/企业状态列）
- ✅ 订单管理列表（支付方式 + 售后状态列）

### 阶段三：详情页优化 — ✅ 完成

- ✅ 会员详情（四段结构 + 树跳转链接）
- ✅ 企业详情（Divider 分区 + 文档状态筛选 + Owner 绑定提醒）
- ✅ 订单详情（Steps 状态流转 + 支付信息 + 优惠信息）
- ✅ 奖励商品编辑（汇总卡片 + SKU 利润提示）

### 阶段四：低频与系统页优化 — ✅ 完成

- ✅ 管理员账号（登录 IP + 超管标识 + 创建时间）
- ✅ 角色权限（复制角色 + 模块全选 + 成员数）
- ✅ 审计日志（目标对象跳转 + scroll.x）
- ✅ 溯源管理（企业选择器化）

---

## 9. 前端实现注意事项

### 9.1 菜单实现

- 一级菜单控制在 5 到 6 组
- 详情页不注册菜单 route
- `ProLayout` 建议从 `mix` 改为纯侧边结构，减少顶部混杂感

### 9.2 页面容器规范

统一建议：

- 列表页：`padding: 24`
- 复杂详情页：`maxWidth` 控制主体宽度
- 表格页默认补 `scroll.x`
- 抽屉和弹窗宽度统一分级

### 9.3 表格规范

- 所有宽表统一加横向滚动
- 操作列放最后并固定宽度
- 列优先级分为：
  - 必看列
  - 辅助列
  - 次要列

### 9.4 配置页规范

- 配置页一律按业务域拆分
- 每组参数都要有说明
- 比例类参数都要有合计校验
- 版本历史和回滚保留

### 9.5 搜索与筛选规范

- 筛选器统一放在表格上方（ProTable `search` 区域），不使用侧边抽屉
- 筛选状态建议同步到 URL query params（`react-router` `useSearchParams`），刷新页面不丢失筛选条件；高频列表页默认遵循，低频页视复杂度决定
- 时间范围筛选默认值（建议，各页面可按业务需要调整）：
  - 订单/发放记录/使用记录：最近 7 天
  - 审计日志：最近 24 小时
  - 统计页：最近 30 天
  - 用户/企业/商品列表：不限
- 模糊搜索输入防抖 300ms，最小输入长度 2 字符；**精确 ID/单号搜索（订单号、用户 ID、退款单号等）不设最小长度限制**
- 每个列表页的筛选项按使用频率排序，高频筛选项默认展示，低频筛选项收入"更多筛选"

### 9.6 响应式断点规范

统一断点定义（与 Ant Design 保持一致）：

| 断点 | 宽度 | 行为 |
|------|------|------|
| `xs` | < 576px | 不支持（管理后台最低 768px） |
| `sm` | ≥ 576px | 侧边栏自动折叠为图标模式 |
| `md` | ≥ 768px | 管理后台最低支持宽度 |
| `lg` | ≥ 992px | 侧边栏展开，详情页单列布局 |
| `xl` | ≥ 1200px | 详情页双列布局，表格完整展示 |
| `xxl` | ≥ 1600px | 页面主体 `maxWidth: 1400px` 居中 |

- ProLayout `breakpoint` 设为 `lg`（992px 以下自动折叠侧边栏）
- 表格横向滚动触发：当列总宽超过容器宽度时自动启用 `scroll.x`
- 树可视化页面（VIP/普通奖励树）：`lg` 以下详情面板自动折叠为抽屉模式

### 9.7 操作反馈规范

| 操作类型 | 确认方式 | 成功反馈 |
|---------|---------|---------|
| 普通 CRUD（创建/编辑/删除） | 删除需确认弹窗，创建/编辑不需要 | `message.success` |
| 审核操作（通过/拒绝） | 二次确认弹窗 + 原因输入 | `message.success` + 列表自动刷新 |
| 资金操作（提现审核） | 二次确认弹窗 + 金额复核 | `notification.success` + 列表自动刷新 |
| 封禁/解封用户 | 二次确认弹窗 + 原因输入 | `message.success` |
| 配置变更 | 二次确认弹窗 + 变更对比展示 | `notification.success` + 显示"已生效" |
| 回滚操作 | 二次确认弹窗 + 影响范围提示 | `notification.success` |
| 批量操作 | 确认弹窗 + 显示影响数量 | `message.success` + 显示成功/失败数量 |

### 9.8 空状态与错误状态规范

- **表格空状态**区分两种情况：
  - 无数据：展示空状态插图 + "暂无数据" + 引导操作按钮（如"创建第一个活动"）
  - 筛选无结果：展示"未找到匹配结果" + "清除筛选条件"按钮
- **接口异常**：
  - 网络错误：展示重试按钮 + 错误描述
  - 403 权限不足：展示"无权限访问此页面，请联系管理员"
  - 404 资源不存在：展示"资源已被删除或不存在" + 返回列表按钮
  - 500 服务端错误：展示通用错误页 + 重试按钮
- **权限不足时的 UI 处理**：
  - 菜单级：无权限的菜单项不显示（通过 `PermissionGate` 控制）
  - 按钮级：无权限的操作按钮隐藏或置灰 + tooltip 提示"无权限"
  - 页面级：直接访问无权限路由时跳转 403 页面

### 9.9 共享组件复用清单

以下结构在多个页面出现，应抽取为共享组件：

| 共享组件 | 使用页面 | 说明 |
|---------|---------|------|
| `TreeVisualization` | VIP奖励树、普通奖励树 | 搜索 + 树渲染 + 详情面板，通过 props 切换主题色和数据源 |
| `AuditReviewModal` | 企业审核、商品审核、提现审核、退款/换货仲裁 | 统一的审核弹窗：通过/拒绝 + 原因输入 + 凭证预览 |
| `ConfigFormGroup` | VIP配置、普通配置、平台设置 | 配置分组表单：标题 + 说明 + 字段列表 + 合计校验 |
| `StatusFilter` | 订单、商品、企业、提现、红包 | 快捷状态筛选 Tag 组，点击切换筛选 |
| `BatchActionBar` | 用户、订单、商品、企业、提现 | 批量选择后的浮动操作栏：已选 N 项 + 批量操作按钮 |
| `AuditTrail` | 会员详情、企业详情、订单详情、配置页 | "查看操作记录"入口，展示审计日志时间线 |

### 9.10 角色与菜单权限映射

不同角色的菜单可见范围建议：

| 菜单组 | 超级管理员 | 运营 | 客服 | 财务 |
|--------|-----------|------|------|------|
| 工作台 | ✅ | ✅ | ✅ | ✅ |
| 用户与奖励 | ✅ | ✅ 用户/会员只读 | ✅ 用户只读 | ✅ 提现审核 |
| 商家与商品 | ✅ | ✅ | ❌ | ❌ |
| 交易与售后 | ✅ | ✅ 订单只读 | ✅ | ✅ 退款只读 |
| 运营活动 | ✅ | ✅ | ❌ | ❌ |
| 系统管理 | ✅ | ❌ | ❌ | ❌ |

- 菜单可见性由前端 `PermissionGate` + 后端 `@Permission()` 双重控制
- 超级管理员绕过所有前端权限检查
- 工作台的待办卡片按角色过滤，每个角色只看到自己负责的待办

---

## 10. 最终目标

整改完成后，管理后台应达到以下状态：

- 左侧目录一眼能看懂业务分区
- 任意页面都能明确回答”这是做什么的”
- 配置页边界清楚，不再出现 VIP 内容混入系统设置
- 高频运营页不会再出现右侧溢出和信息拥挤
- 详情页不再像信息堆叠，而是按任务分区阅读
- 不同角色的管理员看到与自己职责匹配的菜单和操作
- 高频列表页均支持批量操作，运营效率显著提升
- 筛选状态可通过 URL 分享，刷新不丢失
- 空状态和异常状态有明确的引导，不出现白屏或无提示
- 共享组件统一复用，审核弹窗/配置表单/树可视化风格一致

这份文档作为管理后台前端整改的执行基线，后续代码实施应严格对齐本文档的目录归位和页面职责定义。

---

## 11. 剩余整改排程

> 更新于 2026-03-07，基于代码实际状态排定。

### 第一批：高频列表页补列 — ✅ 全部完成

| 页面 | Section | 完成内容 | 文件 |
|------|---------|---------|------|
| ✅ 商家商品 | 6.13 | 分类、库存（SKU 合计）、企业状态列 | `pages/products/index.tsx` |
| ✅ VIP 会员列表 | 6.4 | 可用余额、冻结金额、奖励树层级、自购次数列 | `pages/bonus/members.tsx` |
| ✅ 提现审核 | 6.6 | 提现渠道（Tag）、账户信息、账户类型列 | `pages/bonus/withdrawals.tsx` |
| ✅ 订单管理列表 | 6.18 | 支付方式（Tag）、售后状态列 | `pages/orders/index.tsx` |

### 第二批：页面内容增强 — ✅ 全部完成

| 页面 | Section | 完成内容 |
|------|---------|---------|
| ✅ 退款仲裁 | 6.21 | 退款原因列、支付渠道列（Tag）、卖家处理列、仲裁弹窗增强（订单商品表 + 处理记录 Timeline） |
| ✅ 换货仲裁 | 6.20 | 卖家处理列、收货地址列、仲裁弹窗增强（换货目标商品 + 申请人地址 + 8 条仲裁模板文案） |
| ✅ 普通系统配置 | 6.10 | 三组业务说明（Alert）、推荐模板（50/16/16/8/8/2）、恢复默认值、保存前变更确认弹窗 |

### 第三批：低频页与未整改页 — ✅ 全部完成

| 页面 | Section | 完成内容 |
|------|---------|---------|
| ✅ 登录页 | 6.1 | 去掉默认账号、环境标识 Tag、分状态码错误提示 |
| ✅ 商品编辑 | 6.14 | 表单重排（状态→基础→图片→SKU→审核）、审核记录 Timeline、保存按钮 sticky |
| ✅ 奖励商品列表 | 6.15 | 库存预警（红/橙图标）、毛利列（利润率+金额）、状态快捷筛选、低库存 Switch |
| ✅ 运费规则 | 6.22 | 编辑弹窗冲突检测 Alert、省份选择器预览、优先级 Badge + 行高亮 |
| ✅ 抽奖管理 | 6.27 | Tab 加图标、概率编辑器加宽 960 + 响应式布局、表格 scroll.x、统计面板优化 |
| ✅ VIP 奖励树 | 6.7 | 详情面板可折叠、工具条固定、回到根节点按钮、节点视觉层级（当前/父/孙） |
| ✅ 普通奖励树 | 6.8 | 同 VIP 树结构、绿色主题 #2E7D32、"普通奖励体系" 横幅 |

### 待定项（需后端配套，不在前端排程内）

- 工作台侧边栏菜单徽标（需全局状态方案）
- 批量审核/导出（需后端批量接口 + 导出任务系统）
- 筛选状态同步 URL query params（全局方案，逐页推进）
