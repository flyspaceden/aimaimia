# 平台基金账本实施记录

- 授权：按设计文档实施；子 Agent 使用 GPT 5.6 Luna Max，主 Agent 审查后接受。
- 基线：origin/main@101ada3d97e6f13b19a1aba61fe778f76f20a951。
- 分支：codex/platform-fund-ledgers-20260908。
- worktree：/Users/jamesheden/Desktop/aimaimai-platform-fund-ledgers-20260908。
- 原 staging 脏目录：只读取并复制本次设计文档，其他改动不纳入。

## 交付检查

- [x] 建立干净主干 worktree、独立锁文件依赖环境。
- [x] 核对主干支付利润快照与售后事务入口。
- [x] 公司账本核心与迁移审查。
- [x] 平台全写入口逐笔审计与迁移审查。
- [x] 管理后台、权限、私有凭证与接口对齐。
- [x] 本地 PostgreSQL 迁移、状态机、幂等与并发测试。
- [x] TypeScript、页面实际渲染、资金链路回归。
- [x] 完成主 Agent 审查，设计与实现分别形成范围清楚的本地提交。

未授权 push、CI 发布、测试/生产部署或银行付款。每个验收层独立记录，不以本地结果代替线上验收。

## 最终本地验证（2026-09-08）

- 13 个 Jest 套件、164 项测试通过，其中包含隔离 PostgreSQL 上的公司资金生命周期、平台审计、HTTP 权限与私有凭证集成。
- 覆盖主干 V3 退款专用入口：新公司账独立冲回，既有利润服务已处理的旧用户奖励不重复回收。
- 后端 `npm run build`、管理后台 `npm run build` 通过。管理端保留既有 Expo 根 tsconfig 提示和 bundle 体积提示。
- Prisma validate 通过；从主干 schema 建立全新本机测试库，5 个新迁移按顺序执行成功；数据库到最终 Prisma schema 的 diff 为空。
- 浏览器验证了基金总览、慈善基金逐笔页、创建付款单弹窗。页面渲染使用本地测试数据；资金和权限结论来自独立真实数据库测试，不来自截图。
- 主 Agent 审查并要求修复：合法状态循环被错误去重、流水/账户合并吞事件、VIP 利润快照缺失、预留售后真实付款无法收口、回款跨付款单核销、请求内容冲突、负数 delta 对账失败、已回款重复追偿、权限组合和上传竞态、筛选刷新及历史未知余额展示。
- 数据库添加新账金额约束、不可变事件保护、稳定流水序号；不回写旧 Reward 数据。
- 代码均在指定 worktree，未纳入原 staging 脏目录的其他文件。

## 发布状态

仅本地实现与提交。未 push、未运行远端 CI、未部署测试或生产、未执行真实银行转账。后续发布须按 `docs/operations/platform-fund-ledgers-release.md` 暂停旧消费者并明确切换边界；新账启用后不能直接回滚到旧个人分配代码。

## 2026-09-09 主要问题复审与测试发布

用户授权修复主要问题后推送 staging-next 并测试；用户明确不要求无限扩展审查。本轮修复：非 UTC 数据库会话的时间偏移、基金与公司账本的独立实时权限、付款实际金额/回款凭证/状态历史、平台账户首次创建竞态、释放幂等键归属、VOID 展示和越界分页、审计快照与私有凭证绑定/每日容量限制。未改变历史个人余额和分润比例。

本地完整迁移链成功，backend 274 suites / 3218 tests passed，21 tests 按原条件 skipped；管理后台构建通过。基金 PostgreSQL 验证接入 E2E workflow 的独立 fund_test_ci 数据库。

测试集成使用 codex/fund-ledgers-staging-20260909，基于原 staging-next@de252f78，仅摘取本次基金提交，保留既有测试功能与部署保护。main-based 功能分支独立保留，禁止把测试集成分支整体合入 main。远端 CI 与部署结论以后续发布记录为准。
