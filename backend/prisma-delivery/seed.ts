/**
 * Delivery seed placeholder.
 *
 * Real delivery seed data will be added by the later delivery seed task.
 */
async function main() {
  console.log('Delivery seed placeholder: no-op until real seed data is added later.');
}

main().catch((error) => {
  console.error(error);
  process.exit(1);
});
